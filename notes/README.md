# Notes

Working notes, references, things being figured out. Lower bar than
`decisions/` (no format, no append-only rule), higher bar than scratch
(should be useful to read later).

Things that belong here:
- "What I learned reading the MTLCounterSampleBuffer header"
- "Summary of relevant points from WWDC session XYZ"
- "Open questions about Dynamic Caching that I haven't resolved"

Things that don't:
- Decisions (those go in `decisions/`)
- Experiment results (those go in the experiment's own README)
- Random scratch (don't commit it)
