"""
Experiment 001: Can we time anything at all from Python?

This file is a placeholder. The actual implementation happens on hardware
during the sprint, with intentional minimal scaffolding so we see the raw
behavior of the timing API without our code shaping the answer.

When implementing:
- Don't pre-add averaging, retries, or warmup logic.
- Print/save raw values; do not summarize away the variance.
- If something fails, let it fail loudly. Don't catch exceptions to "make
  it work"; we want to know exactly what doesn't work.
- Save raw output to ./raw/ with a timestamp in the filename.
- Update README.md after the run with: actual setup used (vs. planned),
  result, surprises, new questions.
"""

if __name__ == "__main__":
    raise NotImplementedError(
        "Experiment 001 is pre-registered but not yet run. "
        "Implement on Apple Silicon hardware. See README.md for the question "
        "and intended setup."
    )
